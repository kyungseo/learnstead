# 검증 기록

이 문서는 **내 문서와 대화하는 AI 이해하기 — RAG와 Graph**(가이드)의 명령과 주장을 어디까지 실제로 확인했는지 기록합니다. 튜토리얼과
실습의 실행 기록은 각 자료의 `VALIDATION.md`에 따로 있습니다.

## 현재 요약

| 경로 | 상태 | 확인 범위 |
| --- | --- | --- |
| 문서 구조와 내부 링크 | 통과 | 2026-08-30 로컬 링크·이미지 경로 전수 검사 (3유형 분리 후) |
| 04 §3 임베딩 호출 (`/api/embed`) | 실행 검증 | 2026-08-30 `curl /api/embed` → `2 vectors of 1024 dims` (Ollama 0.33.0, `bge-m3`) |
| 04 §2 점수 구간표 | 실행 검증 | 2026-08-23·30 튜토리얼·실습의 관측값에서 정리 — 정답 0.63~0.73, 무관 0.44~0.50, 단어 겹침 무관 0.61 |
| 06 §3 `ollama ps`·`ollama show` | 실행 검증 | 2026-08-30 `gemma3:4b` 적용 context 4,096 / 선언 상한 131,072 |
| 06 §3 context 초과 입력 조정 | 코드 확인 | 2026-08-30 Ollama 공식 문서와 현재 `server/prompt.go` 확인 — 직접 재현하지 않음 |
| 01·07 Kuzu 개발 중단 | 자료 확인 | 2026-08-30 보도·블로그 (SOURCES) |
| 모델·도구 이름 표 (01·04·05·07·08) | 문서·자료 확인 | 2026-08-30 Ollama·Chroma·reranker·GraphRAG 원형과 변형 공식 자료 재확인 |
| SVG 다이어그램 | 통과 | 2026-08-30 소스 lint, Chromium 2× PNG 렌더, 시각 QA |

## 기준 장비

| 항목 | 값 |
| --- | --- |
| 확인일 | 2026-08-30 |
| 하드웨어 | Apple M4 Pro, 통합 메모리 24GB |
| OS | macOS 26.6.2 |
| 설치 확인 | Ollama 0.33.0, `bge-m3:latest` (`790764642607`, 1,024차원, F16), `gemma3:4b` (`a2af6cc3eb7f`, Q4_K_M) |

## 실행하지 않은 경로를 읽는 법

가이드 본문의 코드 조각 중 위 표에 없는 것(04 §3 Python 조각, 07 §4 Cypher)은 문서 확인 수준이며 이 자료에서는 실행하지 않았습니다.
Python 조각과 같은 호출은 튜토리얼 스크립트가 실제로 수행합니다. Cypher는 문법 예시일 뿐 이 시리즈에서 Neo4j를 실행하지 않습니다.

## SVG 다이어그램 검증 (2026-08-30)

| 항목 | 결과 |
| --- | --- |
| 소스 lint | 15/15 파일 0 error (전용 lint: id·marker 단위·텍스트 폭·레이아웃 계약) |
| 렌더 | Google Chrome 151.0.7922.170 (headless) · 2× PNG · viewBox의 정확히 2배 크기 확인 |
| 시각 QA | fit-to-page와 확대 두 단계로 전건 확인. 라벨·겹침·잘림·화살표 수정 후 재렌더 |
| 본문·alt·title/desc 대조 | 각 SVG의 `<title>`·`<desc>`와 Markdown 대체 텍스트가 같은 범위의 주장을 하도록 작성 |

PNG는 검증용으로만 생성했고 저장소에는 SVG 원본만 둡니다.

## 정적 검증

- 내부 링크·이미지 경로: Markdown 링크·이미지 대상을 파일 존재 여부로 전수 검사
- SVG: XML parsing + 전용 lint
- 한국어 강조 표기: 닫는 `**` 직후에 조사가 붙지 않도록 검사하고 GFM 호환 parser로 render 확인
