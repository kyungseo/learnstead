`src/ledger/report.py`의 `render`에 `top: int | None = None` 인자를 추가하세요. 주어지면 상위 `top`개 분류만 표에 넣고 나머지는 `기타` 한 줄로 합칩니다. 기존 호출은 그대로 동작해야 합니다. `PYTHONPATH=src python3 -m unittest -q`로 통과를 확인하세요. 다른 파일은 건드리지 마세요.
