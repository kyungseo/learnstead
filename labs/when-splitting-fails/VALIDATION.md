# 검증 기록

이 문서는 **나눴더니 틀렸다 — 패턴별 실측과 실패 재현의** 시나리오를 어디까지 실제로 실행했는지 기록합니다.

## 현재 요약

| 경로 | 상태 | 확인 범위 |
| --- | --- | --- |
| 다섯 방식 비교 (`all`, 같은 질문) | 실행 검증 | 2026-08-30 1회 + 단일 3회 반복(시간 편차) |
| 01 라우터 정상·오분류·8문항 `--batch` | 실행 검증 | 2026-08-30 배치 3회 반복 전부 4/8 동일 |
| 01 `--fallback` | 실행 검증 | 2026-08-30 오분류 2문항에서 폴백 후 정답 |
| 01 같은 질문의 파이프라인 분류 | 실행 검증 | 2026-08-30 라우터가 틀린 질문을 파이프라인 분류는 맞힘 |
| 02 워커 격리·취합 왜곡 | 실행 검증 | 2026-08-30 요약 과제 2회, 단일 질문 1회 |
| 02 병렬 vs 순차 (`--max-workers 1`) | 실행 검증 | 2026-08-30 각 2회 |
| 03 루프 통과·수렴 실패·`--stop-on-repeat`·`--budget-tokens` | 실행 검증 | 2026-08-30 각 1회 |
| `OLLAMA_NUM_PARALLEL=3`으로 올린 병렬 재측정 | 실행 검증 · 2026-09-06 | 서버를 `OLLAMA_NUM_PARALLEL=3`으로 다시 올려 병렬 2회·순차 2회. 워커 시간이 겹쳤고(0.6/0.8/1.1초) 벽시계 이득은 0.4~0.7초. 첫 실행(모델 적재 포함 4.5초)은 제외. 실습 02 §3 |
| 다른 모델·다른 과제·문서 10편 규모 | 미검증 | `gemma3:4b` 단일 모델, 문서 3편 규모에서만 |
| 스크립트 문법 | 통과 | 2026-08-30 `py_compile` |
| 문서 구조와 내부 링크 | 통과 | 2026-08-30 전수 검사 |

## 2026-09-07 보강 검증

- 가이드·실습의 단정적 설명을 보정하고 전달 입력의 경계와 전체 토큰 비용을 분리했습니다. `workers`는 코드가 작업을 고정하는 fan-out/fan-in임을 명시했습니다.
- 기존 모델 실행값은 2026-08-30·2026-09-06 기록을 유지합니다. 이번에는 모델 실험을 다시 실행하지 않았으며, 그림의 과거 수치를 본문의 해당 과제에 맞췄습니다.
- `labs/`에서 `python3 -m unittest discover -p 'test_*.py' -v`: 14개 통과. 동시 집계, 진행 중 호출의 예산 초과와 후속 호출 차단, 사용량 누락, 배치 입력·종료 코드, 라우터·평가 JSON, 반복 중단, 패턴별 초기화를 검사했습니다. 고정 응답을 사용하며 실제 모델 품질·SDK/서버 호환성의 검증은 아닙니다.
- Python 3.10 문법 호환 검사·bytecode 컴파일 통과. Markdown 22개 GFM 렌더 후 코드 밖 리터럴 `**` 0건, SVG 7종 lint 0 error·0 warning 및 Chrome 2× 렌더·시각 확인, `git diff --check` 통과.
- 두 팩을 공개 저장소 레이아웃에 임시 배치한 validator 결과: 기존 hero 이미지 4건 누락으로 전체 FAIL. 그 외 링크·형식 문제는 보고되지 않았습니다. 공개 발행 완료를 의미하지 않습니다.
- 토큰 예산은 응답 후 보고된 사용량을 기준으로 다음 호출을 막습니다. 이미 진행 중인 호출은 초과할 수 있고, 단일 프로세스 밖의 요청은 집계하지 않습니다. 사용량 누락 시 예산 활성화 상태에서는 중단합니다.

## 기준 장비

| 항목 | 값 |
| --- | --- |
| 확인일 | 2026-08-30 |
| 하드웨어 | Apple M4 Pro, 통합 메모리 24GB |
| OS | macOS 26.6.2 |
| 설치 확인 | Python 3.14.7(venv), `openai` 3.6.0, Ollama 0.33.0, `OLLAMA_*` 환경 변수 없음 |
| 실행 모델 | `gemma3:4b` (`a2af6cc3eb7f`, Q4_K_M), temperature 0 |
| 적용 context | `ollama ps` CONTEXT 4,096 |

## 실행 결과 (2026-08-30)

### 다섯 방식 비교 — 질문: "관리자는 연차를 며칠까지 이월할 수 있나요?"

| 방식 | 호출 | 입력 토큰 | 출력 토큰 | 시간 | 답 |
| --- | --- | --- | --- | --- | --- |
| single | 1 | 508 | 19 | 0.6초 (첫 호출 4.5초, 반복 1.3·0.6·0.6) | 정답 |
| pipeline | 3 | 365 | 29 | 1.1초 | 정답 (분류 휴가-규정) |
| router | 2 | 268 | 33 | 1.2초 | **틀림** — 회의실-예약으로 분류(`reason` "연차 관련 문의") → "찾지 못했습니다" |
| workers | 4 | 782 | 150 | 3.7초 | 정답 옆에 "규정: 연차 이월에 대한 규정은 없습니다" 병기, 휴가 워커가 "가산하여 이월" 문구 생성 |
| loop | 2 | 1,043 | 45 | 2.3초 | 정답 (1라운드 통과) |

### 라우터 — 8문항 `--batch` (3회 반복, 전부 동일)

| 질문 | 라우터 선택 | 정답 | 판정 |
| --- | --- | --- | --- |
| 연차 이월 한도는? | 휴가-규정 | 휴가-규정 | O |
| 경조 휴가 며칠? | 휴가-규정 | 휴가-규정 | O |
| 관리자는 연차를 며칠까지 이월할 수 있나요? | 회의실-예약 | 휴가-규정 | X |
| 노트북 반납 기한은? | 휴가-규정 | 장비-규정 | X |
| 노트북은 언제까지 반납하나요? | 회의실-예약 | 장비-규정 | X |
| 장비 분실하면? | 회의실-예약 | 장비-규정 | X |
| 회의실 예약은 며칠 전부터? | 회의실-예약 | 회의실-예약 | O |
| 회의실은 몇 시간까지 쓸 수 있나요? | 회의실-예약 | 회의실-예약 | O |

**4/8 = 0.50.** 장비-규정은 한 번도 선택되지 않았습니다. 정상 사례(회의실 몇 시간): 259 토큰·정답. `--fallback`: 노트북 반납 3회·757 토큰·정답,
장비 분실 3회·746 토큰·정답(라우터 `reason`이 106토큰으로 길어짐). 파이프라인의 자유 텍스트 분류는 "노트북은 언제까지 반납하나요?"를 장비-규정으로 맞힘(327 토큰·정답).

**초판(2026-08-23, Ollama 0.32.7)과의 차이:** 당시 5문항 중 2개 오분류(노트북 반납·장비 분실)였고 기준 질문은 맞게 분류됐습니다. 같은 모델
태그·temperature 0에서 버전이 바뀌자 기준 질문까지 틀렸습니다. 두 시점 모두 3회 반복이 동일(결정적).

### 워커 — 요청: "규정 세 편의 핵심을 각각 한 줄로 정리해 줘"

| 실행 | 워커 개별 시간 | 워커 벽시계 | 전체 | 토큰 |
| --- | --- | --- | --- | --- |
| 병렬(동시 3) 1회 | 2.0 / 3.5 / 5.2 | 5.2초 | 8.7초 | 968 / 451 |
| 순차(동시 1) 1회 | 1.5 / 1.3 / 1.2 | 3.9초 | 6.6초 | 968 / 451 |
| 병렬 2회 | 1.5 / 2.7 / 3.8 | 3.8초 | 6.5초 | 968 / 451 |
| 순차 2회 | 1.4 / 1.2 / 1.1 | 3.8초 | 6.5초 | 968 / 451 |

워커 입력 196 / 204 / 229(문서 1편 크기), 취합 339. 취합 출력에서 "15분 이내에 입실하지 않으면 자동 취소" → "15분 내 입실해야 자동 취소"로 뜻이
뒤집힘(2회 모두). 병렬이 순차보다 빠르지 않았고 병렬의 워커 시간이 약 1.2~1.5초 간격 계단 — 서버가 순차 처리한 것으로 판정.

### 루프

| 실행 | 호출 | 입력 / 출력 | 시간 | 결과 |
| --- | --- | --- | --- | --- |
| 사실 질문 | 2 | 1,043 / 45 | 2.3초 | 1라운드 통과 |
| 안내문 (기본, rounds 3) | 7 | 4,339 / 585 | 16.7초 | 상한 도달. 지적: 절차 상세 부족 → 이월 한도 불명확 → 가산·이월 정보 부족 |
| 안내문 `--stop-on-repeat` | 7 | 4,343 / 589 | 15.9초 | 유사도 0.8 미달로 미발동, 상한 도달 |
| 안내문 `--budget-tokens 2500` | 4 | 2,361 / 315 | 8.1초 | 누적 2,676에서 중단 |

### 검증 중 발견한 것

1. 라우터 오분류는 결정적이지만 **버전 간에는 다르다** — 재실행으로 안심할 수 없고, 버전 갱신 뒤 다시 재야 한다.
2. 병렬 스레드 = 병렬 계산이 아니다. 대조 측정 없이는 알 수 없다.
3. 취합 단계는 워커가 맞힌 것을 뒤집을 수 있고, "모른다"를 결과로 승격시킨다. 최종 답만으로는 보이지 않는다.
4. 수렴 실패의 증상(지적이 매번 다름)이 곧 지적-문구 비교 장치를 무력화한다. 예산이 답이다.

## 실행하지 않은 것

- 다른 모델(더 큰 모델, 다른 계열), 문서 10편 이상, 프레임워크 구현과의 비교, 여러 사용자 동시 요청

이 항목들을 확인하면 모델·버전, 질문 묶음, 호출·토큰·시간, 정확도를 기록해 주세요.

## 정적 검증

- 내부 링크·이미지 경로: Markdown 링크 대상을 파일 존재 여부로 전수 검사
- Python: `py_compile` 구문 검사 후 실제 실행
- SVG: 2026-08-23 소스 lint·Chromium 2× 렌더·시각 QA (파일명만 변경, 내용 동일)

## 2026-09-07 Hero 추가 후 최종 검사

- 내장 imagegen으로 hero를 생성하고 한글 제목·구도를 확인한 뒤 WebP로 저장했습니다(1672×941, cwebp 품질 90).
- 기존 hero 누락 4건을 해소했습니다. 두 팩 공개 레이아웃 배치 후 `python3 tools/validate.py --public` 최종 결과: PASS. 앞의 누락·FAIL 기록은 이미지 추가 전 검사입니다.
